# 유니로드 서버 관리 명령어

## ⚠️ 최근 수정 사항
- `config.py`에 `SCORE_CONVERSION_GUIDE_URL` 필드 추가됨
- 서버 실행 에러 해결됨

## 🔴 서버 완전 종료 (권장)

```bash
# 방법 1: killall로 모든 Python 프로세스 강제 종료
killall -9 Python
pkill -9 -f "node.*vite"

# 방법 2: 포트 기반으로 종료 (더 안전)
lsof -ti:8000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```

## ✅ 서버 완전 종료 확인

```bash
# Python 프로세스 확인
ps aux | grep Python | grep -v grep

# 포트 사용 확인
lsof -i:8000
lsof -i:5173

# 둘 다 아무것도 안 나오면 성공!
```

## 🚀 서버 재시작

```bash
# 프로젝트 루트에서
./start.sh
```

## 💡 start.sh가 안 먹힐 때 (수동 실행)

### 터미널 1 - 백엔드
```bash
cd /Users/rlaxogns100/Desktop/Projects/UniZ/backend
python3 main.py
```

### 터미널 2 - 프론트엔드
```bash
cd /Users/rlaxogns100/Desktop/Projects/UniZ/frontend
npm run dev
```

## 🔧 start.sh 개선 (필요시)

start.sh의 pkill이 약해서 프로세스가 남는 문제가 있습니다.
개선된 버전으로 수정하려면:

```bash
# start.sh 맨 위 프로세스 종료 부분을
# 이렇게 바꾸세요:

# 기존 프로세스 강제 종료 (개선)
echo "🧹 기존 서버 프로세스 정리 중..."
killall -9 Python 2>/dev/null
pkill -9 -f "node.*vite" 2>/dev/null
lsof -ti:8000 | xargs kill -9 2>/dev/null
lsof -ti:5173 | xargs kill -9 2>/dev/null
sleep 2
```

## 📊 서버 상태 확인

```bash
# 실행 중인 Python 서버 확인
ps aux | grep "python3 main.py" | grep -v grep

# 포트 사용 확인
lsof -i:8000 -i:5173 | grep LISTEN

# 헬스 체크
curl http://localhost:8000/api/health
```

## 🎯 추천 워크플로우

```bash
# 1. 완전 종료
killall -9 Python && pkill -9 -f "node.*vite"

# 2. 종료 확인
ps aux | grep -E "Python|vite" | grep -v grep

# 3. 포트 확인 (아무것도 안 나와야 함)
lsof -i:8000 -i:5173 | grep LISTEN

# 4. 재시작
cd /Users/rlaxogns100/Desktop/Projects/UniZ
./start.sh
```
